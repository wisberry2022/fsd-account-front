import React from "react";
import ReactDOM from "react-dom/client";
import App from "./0.app/App";
import "@/5.shared/ui/reset.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
